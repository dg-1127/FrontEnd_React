const RejectedButton = () => {
  return (
    <button
      style={{
        backgroundColor: "white",
        color: "#05141f",
        padding: "15px 50px",
        borderRadius: "5px",
        fontWeight: "bold",
      }}
    >
      반려
    </button>
  );
};

export default RejectedButton;
